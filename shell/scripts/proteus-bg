#!/usr/bin/env bash
# proteus-bg — retired Quickshell wallpaper runner.
# Owned proteus-shell paints the BG layer. Kept as a no-op so legacy
# hypr exec-once / Settings apply / pkill paths stay quiet.
exit 0
