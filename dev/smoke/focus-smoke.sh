#!/usr/bin/env bash
# QML chrome/Settings retired — leaf gates moved to settings-next-smoke / shell-smoke / shell-core-smoke.
set -euo pipefail
echo "focus-smoke: SKIP (QML Settings/chrome retired — see settings-next-smoke / shell-smoke)"
exit 0
