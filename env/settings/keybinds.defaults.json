{
  "overrides": [],
  "_comment": "Optional overrides for compositor session binds. Defaults are baked in compositor/src/binds.rs (beacon, lock, workspaces, terminal, …). Each override: { \"id\", \"mods\": [\"super\"], \"key\", \"action\": { \"shell\": [\"chrome\",\"beacon\"] } | { \"dispatch\": \"workspace 2\" } | { \"exec\": [\"proteus-terminal\"] } }. Copy to ~/.config/proteus/keybinds.json to customize; then: proteus-compositorctl dispatch reloadbinds"
}
