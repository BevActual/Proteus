# Prefer wlr for smithay / wlroots sessions; gtk as fallback.
# Hyprland Fact rollback can still use hyprland portal if installed.
[preferred]
default=wlr;gtk;
org.freedesktop.impl.portal.Screenshot=wlr;gtk;
org.freedesktop.impl.portal.ScreenCast=wlr;gtk;
