# Proteus posture profile — host (operate the box) · stub
# v1 ships the file + select path only. Lean/headless chrome comes later.
# Switch: vm/guest/set-hypr-profile.sh host && hyprctl reload

# Placeholder: keep compositor quiet until host profile is designed.
# (No window rules yet — avoid fighting desktop Settings fragments.)
