# Proteus posture profile — host (operate the box) · Phase 1
# Hard flip: proteus-posture host  |  Soft: set-hypr-profile.sh host
# Lean ops: normal windows; Settings/Beacon stay usable; no console fullscreen rules.

# Keep Quickshell chrome unfullscreened (same idea as console exemption).
windowrulev2 = float, class:^(org\.quickshell)$
windowrulev2 = float, title:^(Proteus Settings)$

# Prefer ordinary tiling for ops apps; avoid console-style force-fullscreen.
# (No fullscreen_state rules — host is desk-adjacent, not lean-back.)
