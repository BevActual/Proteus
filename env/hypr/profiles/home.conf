# Proteus posture profile — home (environment controller) · stub
# v1 ships the file + select path only. Hub/panel chrome comes later.
# Switch: shell/scripts/set-hypr-profile.sh home && hyprctl reload

# Placeholder: keep compositor quiet until home profile is designed.
# (No window rules yet — avoid fighting desktop Settings fragments.)
