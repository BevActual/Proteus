# Proteus posture profile — console (legacy filename: media.conf) · stub
# Product name: console. Soft select only today — hard switch (game-scoped
# compositor + sparse shell) is planned; see docs/proteus/POSTURES.md.
# Switch: vm/guest/set-hypr-profile.sh console|media && hyprctl reload

# Placeholder: keep compositor quiet until console session is designed.
# (No window rules yet — avoid fighting desktop Settings fragments.)
