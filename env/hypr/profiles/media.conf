# Proteus posture profile — media (lean-back) · stub
# v1 ships the file + select path only. Real lean-back binds / chrome come later.
# Switch: vm/guest/set-hypr-profile.sh media && hyprctl reload

# Placeholder: keep compositor quiet until media profile is designed.
# (No window rules yet — avoid fighting desktop Settings fragments.)
