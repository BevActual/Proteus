# Active Hyprland posture profile pointer (files remain SoT).
# Soft: vm/guest/set-hypr-profile.sh desktop|console|host|home
# Hard posture flip: vm/guest/proteus-posture desktop|console
# Then: hyprctl reload (soft helper does this)

source = ~/.config/hypr/profiles/desktop.conf
