# Active Hyprland posture profile pointer (files remain SoT).
# Rewrite with: vm/guest/set-hypr-profile.sh desktop|media|host|home
# Then: hyprctl reload

source = ~/.config/hypr/profiles/desktop.conf
