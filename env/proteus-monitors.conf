# Generated by Proteus Settings — Displays
# Apply from Settings → Displays (scale / mode / orientation) writes live monitor = lines.
# Sourced by Hyprland when present in hyprland.conf.
#
# Example:
# monitor = Virtual-1,1920x1080@60,0x0,1,transform,0

# monitor = ,preferred,auto,1
